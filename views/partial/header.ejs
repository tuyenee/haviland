<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Haviland</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/about">About</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/rooms">Rooms</a>
            </li>
            <% if(typeof currentUser != 'undefined' && currentUser.isAdmin()) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/users">Users</a>
                </li>
            <% } %>
        </ul>
        <ul class="navbar-nav">
            <% if(typeof currentUser != 'undefined') { %>
                <div class="dropdown">
                    <button id="userMenu" class="btn btn-outline-primary dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                        <%= currentUser.username %>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/users/<%=currentUser.id%>">Account info</a>
                        <div class="dropdown-divider"></div>
                        <a href="/logout" class="dropdown-item text-danger">Logout</a>
                    </div>
                </div>
                <a href="/logout" class="btn btn-danger ml-3  d-none d-sm-none">Logout</a>
            <% } else { %>
                <li class="nav-item float-right">
                    <a class="nav-link btn btn-primary text-light" href="/login">Login</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>